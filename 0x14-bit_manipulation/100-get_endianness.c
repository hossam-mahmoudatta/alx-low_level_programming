#include "main.h"

/**
 * get_endianness - prints all elements of a linked list
 *
 * Return: no. of nodes printed
 */

int get_endianness(void)
{
	unsigned int i;

	char *sentence = (char *) &i;

	return (*c);
}
