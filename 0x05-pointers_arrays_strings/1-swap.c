#include "main.h"

/**
 * reset_to_98 - a function that takes a pointer to an
 * int as parameter and updates the value it points to 98
 * @a: the tested variable
 * @b: the tested variable
 *
 * Return: None.
 */

void swap_int(int *a, int *b)
{
	*n = 98;
}
